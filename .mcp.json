{
  "$schema": "https://cursor.com/schemas/mcp.json",
  "mcpServers": {
    "turbofy": {
      "url": "https://tdsbhitua7.execute-api.eu-central-1.amazonaws.com/mcp",
      "description": "Turbofy MCP (HTTP) — discover workspaces, edit schema/apps/flows as JSON, edit block sources in a remote build session"
    }
  }
}
